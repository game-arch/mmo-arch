import {environment} from "../environments/environment";
import {PORTS}       from "../../../../../game-servers/lib/constants/ports.constants";

export class Hosts {
    static readonly LOBBY = environment.server;
}
